[workspace]
members = ["live_poll", "poll_reward_token"]
resolver = "2"

[workspace.dependencies]
soroban-sdk = "26.0.0-rc.1"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true
